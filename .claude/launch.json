{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "api",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        "set -a; source .env; set +a; RAG_CONFIG_PATH=configs/baseline.yaml exec .venv/bin/uvicorn src.api.app:app --port 8010"
      ],
      "port": 8010
    }
  ]
}
